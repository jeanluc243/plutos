# Development: local PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/plutos_dev

# Production: define DATABASE_URL in your hosting platform instead of this file.
# Use the Supabase transaction pooler URL (port 6543) and keep sslmode=require.
# DATABASE_URL=postgresql://postgres.PROJECT_REF:PASSWORD@REGION.pooler.supabase.com:6543/postgres?sslmode=require
